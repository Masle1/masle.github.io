name: GitHub Pages

on:
  push:
    branches:
      # The branch where the project source code resides
      # 项目源代码所在的分支
      - main
      # - master
      # - valaxy

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [lts/*]
        os: [ubuntu-latest]
      fail-fast: false
    steps:
      - uses: actions/checkout@v3 # 建议升级到 v3

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8 # 或者指定你本地使用的版本

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3 # 建议升级到 v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm' # 开启 pnpm 缓存，大幅提升安装速度

      # 不再需要 rm -rf node_modules，Actions 运行环境默认是干净的
      
      - name: 📦 Install Dependencies
        run: pnpm install --no-frozen-lockfile

      # 不再需要单独安装 vue，因为 pnpm install 会根据 package.json 自动处理
      
      - name: 🌌 Build Valaxy Blog
        run: pnpm run build

      - name: 🪤 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          force_orphan: true